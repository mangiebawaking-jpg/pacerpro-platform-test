This repositry contains my solution for the platform assessment

It includes:

1. **Sumo logic query +Alert** for detecting slow /api/data API requests
2. **AWS lambda function** to restart the ec2 instance and send and sns notification
3. **terraform** to provision ec2, sns, lambda, and iam permissions